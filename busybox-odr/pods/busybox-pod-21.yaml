apiVersion: v1
kind: Pod
metadata:
  labels:
    appname: appname-3
  name: busybox-21
spec:
  containers:
  - command:
    - sh
    - -c
    - while true; do echo $(date) | tee -a /mnt/test/outfile; sync; sleep 10; done
    image: quay.io/vcppds7878/busybox:latest
    imagePullPolicy: IfNotPresent
    name: busybox
    volumeMounts:
    - mountPath: /mnt/test
      name: mypvc
  volumes:
  - name: mypvc
    persistentVolumeClaim:
      claimName: busybox-pvc-21
      readOnly: false
